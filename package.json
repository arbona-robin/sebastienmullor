{
  "name": "sebastienmullor-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site with JSON content and build scripts for Decap CMS integration",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/dev.js"
  },
  "dependencies": {},
  "devDependencies": {
    "chokidar": "^3.6.0"
  }
}
